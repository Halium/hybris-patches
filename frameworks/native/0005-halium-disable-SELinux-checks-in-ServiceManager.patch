From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: TheKit <nekit1000@gmail.com>
Date: Sun, 7 Mar 2021 17:13:31 +0200
Subject: [PATCH] (halium) disable SELinux checks in ServiceManager

Change-Id: Ida8e91453db58b1123468931c94d4a40d273b0b3
---
 cmds/servicemanager/Access.cpp | 16 +++++++++++++---
 1 file changed, 13 insertions(+), 3 deletions(-)

diff --git a/cmds/servicemanager/Access.cpp b/cmds/servicemanager/Access.cpp
index 6e2abf65e2..f645a96450 100644
--- a/cmds/servicemanager/Access.cpp
+++ b/cmds/servicemanager/Access.cpp
@@ -34,6 +34,9 @@ constexpr bool kIsVendor = false;
 
 #ifdef __ANDROID__
 static std::string getPidcon(pid_t pid) {
+    // Disabled for Halium
+    return "";
+
     CHECK_EQ(nullptr, IPCThreadState::self()->getServingStackPointer())
             << "Did not get context from PID " << pid
             << ". We should always get contexts from other processes.";
@@ -100,9 +103,10 @@ Access::Access() {
     cb.func_log = kIsVendor ? selinux_vendor_log_callback : selinux_log_callback;
     selinux_set_callback(SELINUX_CB_LOG, cb);
 
-    CHECK(selinux_status_open(true /*fallback*/) >= 0);
-
-    CHECK(getcon(&mThisProcessContext) == 0);
+    // Disabled for Halium
+    // CHECK(selinux_status_open(true) >= 0);
+    // CHECK(getcon(&mThisProcessContext) == 0);
+    
 #endif
 }
 
@@ -141,6 +145,9 @@ bool Access::canList(const CallingContext& ctx) {
 
 bool Access::actionAllowed(const CallingContext& sctx, const char* tctx, const char* perm,
         const std::string& tname) {
+    // Disabled for Halium
+    return true;
+
 #ifdef __ANDROID__
     const char* tclass = "service_manager";
 
@@ -162,6 +169,9 @@ bool Access::actionAllowed(const CallingContext& sctx, const char* tctx, const c
 }
 
 bool Access::actionAllowedFromLookup(const CallingContext& sctx, const std::string& name, const char *perm) {
+    // Disabled for Halium
+    return true;
+
 #ifdef __ANDROID__
     char *tctx = nullptr;
     if (selabel_lookup(getSehandle(), &tctx, name.c_str(), SELABEL_CTX_ANDROID_SERVICE) != 0) {
